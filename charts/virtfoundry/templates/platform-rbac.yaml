{{- $platform := .Values.platform -}}
{{- $ratio := int ($platform.kubevirt.cpuAllocationRatio | default 0) }}
{{- /* SA required for kubevirt-config Job when cpuAllocationRatio > 0, not only featureGates/multus/cdi */}}
{{- if or $platform.multus.install $platform.kubevirt.featureGates.enabled $platform.cdi.install (gt $ratio 0) }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.namePrefix }}-platform
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "virtfoundry.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: pre-install,pre-upgrade
    helm.sh/hook-weight: "-20"
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ .Values.namePrefix }}-platform
  labels:
    {{- include "virtfoundry.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: pre-install,pre-upgrade
    helm.sh/hook-weight: "-20"
rules:
  - apiGroups: [kubevirt.io]
    resources: [kubevirt]
    verbs: [get, patch]
  - apiGroups: [apps]
    resources: [daemonsets]
    verbs: [get, list, watch, create, update, patch]
  - apiGroups: [""]
    resources: [namespaces, pods]
    verbs: [get, list, watch, create]
  - apiGroups: [cdi.kubevirt.io]
    resources: [cdis]
    verbs: [get, list, watch, create, update, patch]
  - apiGroups: [apiextensions.k8s.io]
    resources: [customresourcedefinitions]
    verbs: [get, list, watch, create, update, patch]
  - apiGroups: ["*"]
    resources: ["*"]
    verbs: [get, list, watch, create, update, patch, apply]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ .Values.namePrefix }}-platform
  labels:
    {{- include "virtfoundry.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: pre-install,pre-upgrade
    helm.sh/hook-weight: "-20"
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ .Values.namePrefix }}-platform
subjects:
  - kind: ServiceAccount
    name: {{ .Values.namePrefix }}-platform
    namespace: {{ .Values.namespace }}
{{- end }}
