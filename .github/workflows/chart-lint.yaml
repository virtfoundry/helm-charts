name: Chart lint

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Helm
        uses: azure/setup-helm@v4

      - name: Render default values
        run: helm template virtfoundry ./charts/virtfoundry

      - name: Render homelab profile
        run: helm template virtfoundry ./charts/virtfoundry -f ./charts/virtfoundry/values-homelab.yaml
